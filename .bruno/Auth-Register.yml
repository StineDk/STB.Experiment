info:
  name: Auth/Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseUrl}}/auth/register"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
