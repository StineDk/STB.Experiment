info:
  name: Auth/LoggedIn
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseUrl}}/auth/LoggedIn"
  auth:
    type: bearer
    token: "{{JwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
