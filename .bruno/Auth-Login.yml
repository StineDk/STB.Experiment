info:
  name: Auth/Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "Email": "test",
        "Password": "password"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status == 200) {
          const data = res.body;
          bru.setEnvVar("JwtToken", data.token);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
